# תוצאות טסטים — הצעת תיקון להקלדה עברית במצב מקלדת אנגלי (issue #975)

תאריך: 2026-08-26, ענף `feature/english-typo-suggestion-975` (בסיס dev `5d162f27`)
Flutter 3.44.0 / Dart 3.12, Windows 11

## מה נבנה

שני קומיטים:

1. **`125ae259`** — הליבה: `suggestHebrewKeyboardFix` (מיפוי לפי מיקום
   פיזי במקלדת, SI-1452 מול QWERTY) + באנר "האם התכוונת לחפש: ...?"
   במסך תוצאות החיפוש.
2. **`3953d020`** — הרחבה בעקבות בדיקה חיה של המשתמש שחשפה פגם: הבאנר
   עבד על השאילתה המנורמלת, אבל הנרמול מוחק פסיק ונקודה — שהם המקשים
   של ת ו-ץ ("בדיקת" הפך ל"בדיק"). ההצעה עברה לטקסט הגולמי ולזמן
   ההקלדה: ווידג'ט משותף `TypingLayoutFixSuggestion` בדיאלוג החיפוש,
   בשדה איתור הספרים בספרייה ובאיתור המקורות.

העיקרון בכל המסלולים: **הצעה בלבד** — הטקסט של המשתמש לעולם לא מוחלף
ולא מורחב אוטומטית; ההחלפה רק בלחיצה מפורשת.

3. **`38cd03cc`+** — נגישות מקלדת בסגנון Gmail (בקשת המשתמש): כשההצעה
   מוצגת, Tab בשדה מעביר את הפוקוס אליה, Enter/רווח מחיל ומחזיר את
   הפוקוס לשדה — בשלושת המסכים. רק Tab נקי מיורט: Ctrl+Tab (מעבר
   טאבים), Shift+Tab והקפיצה לרשת הספרים בספרייה לא נפגעים (מקובע
   בטסטים); ה-handler משוחזר ב-dispose.

## טסטים

17 טסטים חדשים בשלושה קבצים, כולם עוברים:

- `test/search/hebrew_layout_suggestion_test.dart` (10) — המיפוי עצמו,
  כולל הדוגמה מגוגל (",heui veksv ctbdkh," → "תיקון הקלדה באנגלית"),
  אותיות סופיות מהמקש עצמו, ותנאי אי-ההצעה (עברית קיימת, אות בודדת,
  תוצאה בלי עברית).
- `test/search/layout_fix_suggestion_banner_test.dart` (4) — הבאנר מוצג
  רק כשיש הצעה, ולחיצה — ורק היא — מוסרת את הטקסט המומר.
- `test/search/typing_layout_fix_suggestion_test.dart` (3) — ההצעה החיה
  מופיעה/נעלמת עם ההקלדה, פסיק ונקודה נכללים ("cshe," → "בדיקת"),
  ולחיצה מחליפה את השדה עם סמן בסוף.

`flutter analyze` על כל שבעת הקבצים שנגעו בהם — נקי.

## אימות ויזואלי (build debug על Windows)

- איטרציה 1 אומתה חיה ע"י המשתמש: הקלדת "בדיקת חיפוש החלפת אותיות"
  במצב אנגלי → הבאנר הופיע במסך התוצאות — וחשף את פגם הנרמול (ההצעה
  יצאה "בדיק... החלפ..." בלי ת) שתוקן באיטרציה 2.
- איטרציה 2 אומתה חיה ע"י המשתמש בשלושת המסכים, על build debug מעודכן:
  - איתור מקורות — "crtah," מציע "בראשית" תוך כדי הקלדה
    ([975-findref-typing.jpg](img/975-findref-typing.jpg))
  - דיאלוג החיפוש — "cshe, thu, tbdkh," מציע מתחת לשדה
    ([975-dialog-typing.jpg](img/975-dialog-typing.jpg))
  - מסך התוצאות — ההזדמנות השנייה עם ההמרה המלאה כולל האותיות שמאחורי
    פסיק/נקודה: "בדיקה של שגיאה בחיפוש"
    ([975-results-banner.jpg](img/975-results-banner.jpg))

## סוויטה מלאה

`flutter test` (אחרי איטרציה 2): **10,324 עברו, 7 דולגו, 8 נכשלו** —
כולם קיימים-מראש וסביבתיים, אף אחד לא באזור השינוי:

- אותם שישה מהענפים הקודמים: bash ב-release_packaging, סף כיווץ SQLite
  (3× compaction), נעילת קובץ DOCX, מפריד נתיבים ב-custom_folder_model.
- `settings_l10n_test` — תלוי-מכונה (הגנרטור המקומי שונה מהריפו).
- `settings_bloc_test` — הבהוב תחת מקביליות הסוויטה בלבד.

(גם הרצת ביניים אחרי איטרציה 1 בלבד: 10,321 עברו, אותם 8.)

אחרי תוספת נגישות המקלדת: **10,328 עברו, 7 דולגו, אותם 8** — כולל 13
טסטי הווידג'ט (Tab קופץ להצעה, Enter מחיל ומחזיר פוקוס, Ctrl+Tab לא
נבלע, Tab רגיל כשאין הצעה, הסרת ה-handler ב-dispose).
הערה: `search_scope_menu_search_actions_test` הבהב פעמיים בהרצה
ממוקדת של תיקיית test/search — נבדק ונכשל באותה צורה גם בלי השינוי
(stash), ובסוויטה המלאה עבר.
