# issue #1134 — כרטיסיות בצד: הנעץ נדחק אחרי אייקון ה-PDF

תאריך: 2026-09-05 | ענף: `fix/side-tabs-pin-before-type-icon-1134`

## הבאג שאומת

ברצועת הכרטיסיות האנכית (`VerticalReadingTabStrip`) שורת הכרטיסייה בנתה את
אייקון סוג הכרטיסייה (PDF / כלי) ואחריו את הנעץ, ולכן בכרטיסיית PDF מוצמדת
הנעץ נדחק שמאלה ולא התיישר עם הנעצים של שאר הכרטיסיות.

## הפתרון

הנעץ עובר לקצה השמאלי של השורה (אחרי הכותרת, לפני ה-X שמופיע בריחוף/בחירה).
אייקון הסוג נשאר בקצה הימני. כך הנעצים של כל הכרטיסיות המוצמדות מיושרים
באותו קו, ללא תלות בסוג הכרטיסייה. שינוי של סדר בלבד, ללא קוד חדש.

## אימות ויזואלי

בנייה מקומית (Windows, debug) עם ארבע כרטיסיות מוצמדות בצד, אחת מהן PDF:

| מצב | תיאור | תמונה |
|---|---|---|
| לפני (dev) | בכרטיסיית ה-PDF הנעץ נדחק אחרי אייקון ה-PDF; שאר הנעצים בקצה הימני | `img/1134-before.png` |
| אחרי (הענף) | כל הנעצים בקצה השמאלי, מיושרים; אייקון ה-PDF נשאר בקצה הימני | `img/1134-after.png` |

## בדיקות

`test/navigation/reading_tabs_side_panel_test.dart` — בדיקה חדשה: כרטיסיית PDF
מוצמדת וכרטיסיית טקסט מוצמדת ← אייקון ה-PDF מימין לכותרת, הנעץ משמאל לכותרת
בשתיהן, ושני הנעצים על אותו קו אנכי. הקובץ קיבל `Settings.init` (PdfBookTab
דורש אותו).

`flutter analyze`: נקי. `test/navigation/reading_tabs_side_panel_test.dart`:
**29 עברו, 0 נכשלו**.
