# אבחון ביצועים ומשאבים — ממצאים

**תאריך:** ג' תמוז תשפ"ו (יולי 2026) | **מכונה:** i5-8350U ‏(4C/8T), ‏16GB RAM | **מצב:** profile build

## עדכון: מדידה חוזרת אחרי קומיט האופטימיזציות f80c85615

נמדדו 3 הרצות מלאות + תחקור heap אחרי הקומיט (המכונה הייתה בשימוש פעיל — ההשוואה האמינה
היא שניות-ליבה של תהליך otzaria ומדדי פריימים פנים-תהליכיים, לא זמני קיר).

| מדד | לפני | אחרי (3 הרצות) | שינוי |
|---|---|---|---|
| גלילה: buildP95 | 75.5ms | 22–32ms | **‎-57% עד ‎-71%** |
| גלילה: ג'אנק | 36% מהפריימים | 4–16% | **‎-55% עד ‎-89%** |
| החלפת טאב: buildP95 | 98.9ms | 40–44ms | **‎-55%** |
| פתיחת 5 ספרים: buildP95 | 97.5ms | 47–71ms | **‎-28% עד ‎-52%** |
| החלפות טאבים: שניות-ליבה | 5.6 | 4.1–5.4 | ‎-18% בממוצע |
| חיפוש שני: שניות-ליבה | 0.8 | 0.2–0.4 | ‎-63% |
| גלילה: שניות-ליבה | 10.2 | 9.6–11.1 | ללא שינוי* |

\* המטמון המיר עבודה מבוזבזת בפריימים שמסופקים בפועל (‏115→136–158 פריימים באותה גלילה) —
אותו CPU, חוויית משתמש חלקה בהרבה. ה-RAM עם 6 טאבים נשאר ‏~550MB — מכוון: הספר המלא נדרש
בזיכרון לחיפוש בתוך הספר (ההמלצה המקורית לשחרר תוכן טאבים ברקע נפסלה מסיבה זו).

**תצוגת-ספר PDF (כיסוי חדש):** נוספו שלבי `open_pdf_book_view`/`pdf_book_view_flips` לתרחיש.
5 דפדופי כפולות שרדו ללא קריסה (התור הסדרתי + ‏renderScale 1.5 עובדים); שיא RSS עם שני טאבי
PDF פתוחים: ‏907MB. הקריסה המקורית אירעה דווקא ב-regularView (עומס tiles של pdfrx בדפדוף
מהיר תוך טעינה פרוגרסיבית) — לא שוחזרה מאז.

**ממצא חדש — לולאת repaint אינסופית בתצוגת-ספר:** במנוחה עם טאב תצוגת-ספר פעיל האפליקציה
מרנדרת ברציפות ‏~48fps‏ (‏402 פריימים ב-8.4 שניות idle, ‏~29% CPU) לעומת שקט מוחלט ב-PDF רגיל.
המנגנון: `_clampMatrixToSpread` ‏(`pdf_book_screen.dart:1897-1935`) מחזיר תמיד מטריצה חדשה
מ-`calcMatrixFor` גם כשאין תיקון ⇒ עדכון controller ⇒ ‏repaint ⇒ נורמליזציה שוב.
הבדיקה `_wasMatrixClamped` (אפסילון, שורות 1937-1948) קיימת אך אינה בשימוש במסלול הזה.
תיקון מינימלי: להחזיר את `matrix` המקורית כשהסטייה מתחת לאפסילון.

תחקור heap חוזר: ללא דליפה גם אחרי הקומיט — ה-classes חוזרים בדיוק לבסיס אחרי סגירת טאבים.
הרצות ההשוואה: לפני `run_20260703_035203`; אחרי `run_20260703_184908`, `…_185509`, `…_190021`
(האחרונה כוללת את שלבי תצוגת-הספר).

## עדכון 2: צורת הדף, מפרשים מרובים, הדפסה (הרצות 20260704)

התרחיש הורחב בשלבים: `page_shape_open`, `all_commentators_open` (115 מפרשים),
`scroll_all_commentators`, `print_preview`. מדידות (מכונה בשימוש — להסתמך על שניות-ליבה):

| שלב | שניות-ליבה | RAM | הערות |
|---|---|---|---|
| צורת הדף (פתיחה+מנוחה) | ~2 | נמוך | הטעינה ההתחלתית איטית (עשרות שניות: ספרי העזר נטענים במלואם), אבל מצב יציב שקט וזול |
| הפעלת כל 115 המפרשים | 3.4-3.5 | +15MB | פיק 233-269% בבניית הקישורים |
| גלילה עם כל המפרשים | **8.4-8.7** | **+68MB** | כמו גלילה רגילה ב-CPU, אבל תוספת RAM ניכרת (מטמון תוכן + widgets) |
| תצוגה מקדימה של הדפסה | 4.5-5.1 | **פיק +100MB** | פיקים 251-267%; הזיכרון משוחרר אחרי סגירה |

ממצאי קוד עיקריים (ניתוח סטטי מאומת):
- **צורת הדף**: כל חלונית מפרש טוענת את *כל* ספר המפרש ל-`_fullCommentaryCache` — מטמון
  `static` **ללא פינוי לעולם** (`page_shape_screen.dart:1364`); עד 5 רשימות גלילה חיות במקביל;
  כל emission של ה-bloc מפעיל סנכרון בכל החלוניות.
- **הדפסה**: רסטריזציית שורות מנוקדות רצה **על ה-main isolate** ב-pixelRatio 3
  (`pdf_text_rasterizer.dart:14-56`); טרנספורמציות טקסט רצות על **כל הספר** גם כשנבחרו 3 שורות
  (`printing_screen.dart:782-804`); כל שינוי הגדרה מריץ את כל הצינור מחדש; N-up = מעבר רסטר שני מלא;
  עד 60 עמודי preview כ-PNG ב-2x בזיכרון.
- **מפרשים מרובים**: קבוצות נפתחות כברירת מחדל וכל קבוצה מרנדרת את *כל* הקישורים שלה בבת אחת
  (`commentary_list_base.dart:163,1932`) — פרסור HTML + שליפת DB פר-קטע, פרופורציונלי ל-N;
  ‏LRU התוכן קבוע על 400 רשומות ומתרוקן בגלילה עם N גדול (‏thrash → שליפות DB חוזרות,
  `links.dart:23`); ‏`_displayReferenceCache` בלתי-מוגבל (`links.dart:24`).

המלצות צמצום לתחומים אלה (לפי תמורה/מאמץ):
1. **הדפסה**: ✓ חיתוך לטווח לפני הטרנספורמציות + מטמון base-PDF (קומיט 20269a8fa).
   העברת רסטריזציית הניקוד ל-isolate **נפסלה** — dart:ui לא זמין ב-Isolate.run.
   ✓ רזולוציית ה-preview הורדה 2x→1.5x (משפיע רק על התצוגה; הפלט וקטורי).
2. **מפרשים**: ✓ `_displayReferenceCache` נחסם (LRU 1000, בתבנית `_contentCache`).
   עדיין פתוח: ברירת מחדל מכווצת לקבוצות כש-N גדול (החלטת מוצר); LRU תוכן דינמי לפי N.
3. **צורת הדף**: ✓ `_fullCommentaryCache` נחסם (LRU 8 ספרים). עדיין פתוח: טעינת חלון
   במקום ספר מלא לכל חלונית (מסלול `getBookQuickPreview` כבר קיים).

**רינדור רציף בטאבי PDF (חקירה, סוכם):** שלושה מרכיבים זוהו ונפתרו/תוחמו:
1. אנימציות בטאבי רקע חיים (keepAlive) המשיכו לתזמן פריימים — **תוקן** בעטיפת ילdi ה-PageView
   ב-`TickerMode(enabled: i == validIndex)` ‏(`reading_screen.dart`). אומת: דפדוף PDF רגיל חזר
   מ-39fps רציף ל-10fps לפי-דרישה, ושני טאבי PDF במנוחה שקטים (~7fps).
2. `_clampMatrixToSpread` החזיר תמיד מטריצה חדשה — **תוקן** באפסילון (היגיינה; לא היה מקור הלולאה).
3. שארית: אחרי דפדופים במסמך ענק, ספינר הייצוב (`_waitingForStableLayout`, מוצג עד
   `onDocumentLoadFinished`) יכול להישאר דקות במכונה איטית עם שני מסמכים על worker יחיד של
   pdfrx — ‏~50fps של ספינר מסך-מלא. **מוקטן**: הספינר עצמו נעטף ב-RepaintBoundary, כך
   שכל טיק שלו מרסטר רק את שכבת הספינר הקטנה — לא את רקע ה-overlay ולא את שכבת
   ה-viewport (כולל ה-ColorFiltered). הפריימים עדיין מתוזמנים, אבל עלותם ירדה משמעותית.

---

## מתודולוגיה

הרנס אבחון (`tool/perf_probe/`) מריץ את האפליקציה האמיתית עם ספריית הספרים המלאה ומדמה
שימוש: עלייה, פתיחת 6 ספרי טקסט, גלילה, החלפות טאבים, ניווט מסכים, שני חיפושים גלובליים,
סגירת טאבים, פתיחת PDF (ברכות) ודפדוף. במקביל: דוגם CPU/RAM חיצוני (כל 500ms), מדידת
frame timings ו-RSS מבפנים, וצילומי heap דרך ה-VM service. הרצות הבסיס:
`%TEMP%\otzaria_perf_probe\run_20260703_035203` (תרחיש מלא) ו-`run_20260703_040541` (תחקור heap).

## מדידות עיקריות (לפי שלב)

| שלב | CPU ממוצע | שניות-ליבה | RAM | פריימים |
|---|---|---|---|---|
| עלייה עד המסך הראשי | 167% | 1.9 | 157→307MB | תקין |
| בניית קטלוג | 331% (פיק) | 1.7 | +16MB | — |
| חימומים אחרי עלייה | פיק 209–427% | 2.8–7 | +12–40MB | — |
| **גלילה בספר** | **151%** | **10.2 — הגבוה ביותר** | +93MB | **36% ג'אנק, buildP95=76ms** |
| פתיחת 5 ספרים | 217% | 6.8 | +55MB | buildP95=98ms |
| החלפות טאבים | 38% | 5.6 | — | buildP95=99ms |
| חיפוש גלובלי | 14% | 0.9 | +21MB | תקין ✓ |
| פתיחת PDF | 143% | 6.7 | **+164MB** | תקין |
| דפדוף PDF | פיק 226% | 5.9 | פיק 707MB | תקין |

**זיכרון כולל:** ‏~335MB אחרי עלייה → ‏~546MB עם 6 טאבי טקסט → ‏פיק 707MB עם PDF.

## תחקור "זיכרון שלא משתחרר" — מסקנה: אין דליפה

צילומי heap עם GC כפוי (baseline → 6 טאבים → אחרי סגירת הכול):

| נקודה | RSS | ‏Dart heap חי | עדות |
|---|---|---|---|
| בסיס | 357MB | 83MB | — |
| 6 טאבים פתוחים | 495MB | 130MB | ‏`Link`‏×56,807 ‏(5.2MB), ‏`SemanticsConfiguration`‏×15,011, ‏+12MB strings |
| אחרי סגירה + GC | 492MB | (טרום-GC ‏125) | ה-classes חזרו בדיוק לבסיס |
| ‏8 שניות אחר-כך + GC | 420MB ↓ | **67.5MB** (מתחת לבסיס) | אין שרידים |

ה-heap החי חוזר לחלוטין לרמת הבסיס אחרי סגירת טאבים — **אין אובייקטים דולפים**. ה-RSS הגבוה
שנשאר זמנית הוא זבל שממתין ל-GC + עמודי heap שה-VM מחזיר ל-OS בהדרגה (ירידה נמשכת גם אחרי
המדידה). ההשקעה צריכה להיות בהקטנת מה שמוחזק *בזמן* שהטאבים פתוחים, לא בחיפוש דליפות.

## הצרכנים המרכזיים והשורש בקוד

### 1. גלילה: רינדור HTML מחדש בכל build — צרכן ה-CPU מס' 1
כל קטע נראה עובר בכל build את שרשרת ה-regex המלאה (ניקוד, טעמים, שמות קדושים, הדגשות) ופרסור
HTML מאפס, ללא מטמון: `lib/widgets/smart_text/smart_text_widget.dart:50` ←
`TextRendererService.render` ‏(`text_renderer_service.dart:16-67`). בנוסף, סינון `state.links.where(...)`
פר-קטע פר-build ‏(`combined_book_screen.dart:1686`).

### 2. חימום רקע: הספר המלא ב-RAM לכל טאב + rebuild לכל chunk
`_warmContentCacheInBackground` ‏(`text_book_bloc.dart:1986-2049`) טוען את כל הספר ב-chunks של
640 שורות לכל טאב פתוח. כל chunk משנה את `content.length`/`contentVersion` שנמצאים ב-props של
ה-state ‏(`text_book_state.dart:469-470`) ⇒ rebuild מלא של ה-viewport (כולל סעיף 1) עשרות פעמים
לכל ספר. זה מקור ה-217% CPU בפתיחת ספרים וחלק ניכר מ-+35–60MB לטאב.

### 3. כל הטאבים חיים לנצח
`PageView` בונה child לכל טאב (`reading_screen.dart:332-338`) + `wantKeepAlive => true`
‏(`text_book_screen.dart:130-133`, וכן ב-PDF). ‏N טאבים = N ספרים מלאים + N רשימות קישורים +
N עצי widgets/semantics חיים (נמדד: ‏15,011 ‏SemanticsConfiguration עם 6 טאבים).

### 4. PDF: פרה-רנדר כפולות ב-scale 2.0 + כל קישורי הספר בזיכרון
במצב תצוגת-ספר, כל עדכון controller מפעיל פרה-רנדר של עד 3 כפולות ב-פי-2 מהגודל הטבעי כ-bitmaps
נייטיביים (`pdf_book_screen.dart:2155-2162`, טריגר ב-`:2929`) — בהרצה הראשונה זה כנראה מה שהקריס
את התהליך (מוות נייטיבי ללא dump, ‏RSS ‏~600MB, דפדוף מהיר). בנוסף נטענת כל רשימת הקישורים של
הספר לטאב (`pdf_book_screen.dart:2823-2825`) אף שקיים API לפי טווח (`getLinksForBookRange`).

### 5. חיפוש: עץ קטגוריות לא-lazy + re-search מלא על כל שינוי facet
מנוע Rust מהיר וזול (14% CPU) — העלות ב-UI: העץ נבנה כולו ב-`SingleChildScrollView` עם
`TextPainter.layout()` לכל צומת, בכל emission ‏(`full_text_facet_filtering.dart:529-576`), וכל
לחיצה על קטגוריה שולחת `UpdateSearchQuery` מחדש = חיפוש + ספירת facets מלאים
(`search_bloc.dart:439-468`).

### 6. אתחול (משני — כבר קיימת דחייה טובה אחרי חשיפת החלון)
בניית הקטלוג (~1.7 שניות-ליבה, ‏7,316 ספרים) על ה-main isolate; אשכול חימומים (BooksCache /
AcronymsCache / GenerationCache / ReferenceBooksCache / מילונים) עם סריקות טבלה מלאות + regex
פר-שורה על ה-main isolate; ‏BooksCache קורא את טבלת `book` פעם שנייה אחרי שהקטלוג כבר קרא אותה;
‏SQLite מוקצה 50MB cache + ‏64MB mmap קבועים (`seforim_repository.dart:79-84`).

## המלצות מדורגות (סטטוס נכון ל-f80c85615)

**בוצע ✓** (ראו פרק "עדכון" למעלה):
1. ✓ מטמון HTML מעובד לפי (טקסט, הגדרות) — `TextRendererService.processText` עם LRU ‏4MB.
2. ✓ ביטול rebuild פר-chunk בחימום — צבירה ושליחה באצוות של 8 chunks.
3. ✓ ‏PDF: ‏renderScale ‏1.5 + תור prerender סדרתי עם דה-דופ.
4. ✓ הסרת `TextPainter.layout` פר-צומת בעץ ה-facets (מטמון overflow) + הסרת
   `BlocBuilder<SettingsBloc>` מעל מסך הקריאה.
5. ✓ ‏`linksByLine` במקום סינון כל קישורי הספר פר-שורה.

**נפסל ✗:**
- שחרור `content` בטאבים ברקע — החיפוש בתוך הספר דורש את התוכן המלא בזיכרון. החימום המלא
  נשאר בכוונה.

**עדיין פתוח:**
6. לולאת ה-repaint בתצוגת-ספר (הממצא החדש למעלה) — התיקון הזמין ביותר כעת.
7. קישורי PDF לפי טווח במקום כל הספר (`getLinksForBookRange` קיים; ‏56,807 מופעי Link ל-6 ספרים).
8. עץ facets ‏lazy (ילדים רק בהרחבה) + לחיצה על קטגוריה כסינון בצד לקוח במקום re-search מלא.
9. אתחול (חלקי): **איחוד קריאת טבלת הספרים הכפולה — בוצע ✓**. בניית הקטלוג
   ‏(`database_library_provider.dart:loadBooks`) זורעת את `BooksCache`, וכשהזריעה
   מקדימה — אין קריאה שנייה. כשה-warmUp מקדים את הקטלוג, השאילתה רצה ב-isolate
   המשותף (`find_ref_db_isolate.dart`) ולא על ה-main. גם ה-TOC של פתיחת ספר
   ‏(`sqlite_data_provider.getBookTocFromDb`, "שמור וזכור") עבר לאותו isolate —
   שתי השאילתות היו ANRים בייצור. **נשאר פתוח:** בניית הקטלוג עצמה
   (`getAllBooksWithRelations`, ~1.7 שניות-ליבה) עדיין על ה-main isolate;
   מילון/ראשי-תיבות בטעינה עצלה.

   **שני מחירים שנוצרו בהעברה, ולא נמדדו:**
   - **תור סדרתי:** ה-worker מעבד בקשות בזו אחר זו (`processingChain`).
     פתיחת ספר שמגיעה בזמן `prewarmAltTocFlat` (בנייה של 61k+ ערכים, מתועדת
     כ-1-2 שניות) תמתין לסיומו. זו החלפת ANR בהשהיית פתיחה — לא בהכרח פסולה,
     אבל לא נמדדה. אם תימדד כמורגשת, הפתרון הוא עדיפות/תור נפרד לשאילתות
     קצרות, לא החזרה ל-main isolate.
   - **RAM:** ה-worker חי מעכשיו כמעט תמיד (כל פתיחת ספר), ולא רק אחרי שימוש
     ב"איתור מקורות". לכן 50MB cache + 64MB mmap של החיבור השני הם תוספת
     כמעט-קבועה במובייל — בדיוק המכשירים שבהם נמדדו ה-ANRים.

## הרצה חוזרת (למדידת שיפור מול קו הבסיס)

```powershell
pwsh tool\perf_probe\run_perf_probe.ps1                 # תרחיש מלא (מגבה ומשחזר את נתוני המשתמש)
pwsh tool\perf_probe\run_perf_probe.ps1 -Target integration_test/memory_retention_probe_test.dart  # תחקור heap
dart run tool\perf_probe\analyze.dart <run-dir>          # דוח
```
